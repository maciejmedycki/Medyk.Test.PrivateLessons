namespace Medyk.Test.PrivateLessons.AutoFixture
{
    public class Strings
    {
        public string JoinWithSpace(string one, string two, string three)
        {
            return $"{one} {two} {three}";
        }
    }
}